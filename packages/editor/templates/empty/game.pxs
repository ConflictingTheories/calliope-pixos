/**
 * Template: Empty Project
 * A blank slate to start your game from scratch
 */

// Global game configuration
const CONFIG = {
  WIDTH: 320,
  HEIGHT: 180,
  SCALE: 2,
  TARGET_FPS: 60,
};

// Main game loop entry point
function main() {
  print("Welcome to PixoSpritz!");
  
  -- TODO: Add your game logic here
  -- - Create sprites
  -- - Handle input
  -- - Update game state
  -- - Draw to screen
}

-- Handle game input
function onKeyPress(key)
  if key == "esc" then
    pixos.quit()
  end
end

-- Called when a sprite collides with another
function onCollision(a, b)
  print("Collision between " .. a.name .. " and " .. b.name)
end
