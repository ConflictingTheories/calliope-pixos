
# GitHub Sponsors (uncomment if enabled)
# github: [your-github-username]

# Ko-fi username (only the username, not the full URL)
ko_fi: kylederbymac

# Buy Me a Coffee username (only the username, not the full URL)
buy_me_a_coffee: kylederbymac

# If you want a single custom link (not needed if using above):
# custom: ["https://your-custom-donation-link.com"]
